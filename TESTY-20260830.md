# A2-037 - vysledok testov a upratanie .pyc (30.8.2026)

## Testy

Prikaz: `python -m pytest -q` v korene repozitara (Python 3.12, Windows).

```
..................................................                       [100%]
50 passed in 1.58s
```

**50 passed, 0 failed.** Zbierka: `tests/test_charge.py`, `test_input.py`,
`test_parse.py`, `test_preflight.py`, `test_scraper.py` + `conftest.py`.

### Co bolo treba pred behom

Prvy pokus skoncil `ERROR tests/test_charge.py` -> `ModuleNotFoundError: No
module named 'psutil'` uz pri zbere testov (`import main` -> `from apify import
Actor` -> `crawlee` -> `psutil`). Nie je to chyba testov ani kodu, ale chybajuca
tranzitivna zavislost v lokalnom Python 3.12 prostredi. Po `python -m pip install
psutil` presla cela sada. `requirements.txt` som nemenil - `psutil` si tahá
`apify`/`crawlee` samo, v Docker image je pritomny.

## .pyc v repozitari

`.gitignore` uz obsahuje `__pycache__/` aj `*.pyc` a `git ls-files` nesledoval
ani jeden `.pyc` - do gitu sa teda nikdy nedostali. Na disku vsak lezalo 81
skompilovanych suborov v troch `__pycache__` adresaroch (`src/`, `src/tg/`,
`tests/`), vratane bytecode z Pythonu 3.12 aj 3.14.

Vsetky tri adresare su presunute (nie zmazane) do:
`C:\Users\PC\Desktop\Marek-Projekty\_to_delete\telegram-pycache-20260830\`

Po presune: `0` suborov `.pyc` v repozitari, `git status --porcelain` prazdny.
Pytest si `__pycache__` pri dalsom behu vytvori znova - `.gitignore` ich uz drzi
mimo gitu, takze staci nechat byt.
